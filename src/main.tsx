import { render } from 'preact';
// import DisableDevtool from 'disable-devtool';
import App from './App';

// DisableDevtool(); <-- TODO: Enable on developement
render(<App />, document.getElementById('root')!);
